# Phase 5 — FastAPI Backend Dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
sqlalchemy>=2.0.0
# Postgres 드라이버 — bare 'postgresql://' URL 의 기본 다이얼렉트(psycopg2)용.
# 없으면 DATABASE_URL=postgresql://... 일 때 import 시점에 ModuleNotFoundError 로 startup 크래시.
psycopg2-binary>=2.9.0
# pgvector — Postgres 벡터 타입(question_embeddings). 순수 파이썬이라 시스템 의존성 없음.
pgvector>=0.3.0
# Google Gemini 임베딩(gemini-embedding-001) — question_embeddings 적재용
google-genai>=1.0.0
python-multipart>=0.0.9
pydantic[email]>=2.0.0

# LLM
groq>=0.9.0

# ML/Data (already installed from Phase 1-4)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
torch>=2.0.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
